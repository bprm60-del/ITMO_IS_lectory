(Инструкции - можешь пользоваться учебниками из интернета и достоверными источниками по математике, если не уверен  в достоверности можешь спросить меня. Дополняй информацию согласно всем предыдущим инструкциям)
Бинарной операцией назовём операцию $M*M -> M$ 
(m1,m2) -> m1*m2
Допустим бинарная операция $N*N -> N$
(n1,n2) -> n1-n2 - не бинарная операция (приведи примеры ещё действительно бин операций)
Определение моноиды: непустое множество M с бинарной операцией n
Дальше приводи примеры и дополняй данные
Свойства моноида
1) Ассоциативность $(a*b)*c = a*(b*c)$
2) Существование единичного элемента $\exists e \in M : e*a = a*e = 1 => \forall a \in M$ 
	(докажи единственность)
3) Коммутативность $a*b=b*a$ $| \forall a, b \in M$ (коммутативный моноид (абелев))
(Допустим моноид Z (коммутативный моноид) с операцией сложения)
(Допустим моноид N (комм моноид) по операции умножения)

Этот раздел поясни больше всего
Пусть X - мн-во M = Maps(X,X) - все функции из X->X (логично что это будут просто все пары)
$M*M \cdot -> M - комм$ 
(f, g) |-> $f\cdot g$ 
X-> (g) X -> X -> (f) X тогда существует функция X->($g \cdot f$) X
Присутствует ассоциативность, нейтральный элемент
X = $(a,b,c)$ 
Maps(X,X)
$\sigma_1(a|-> b, b|-> c| c|-> a)$  
$\sigma_2 (a|-> b, b|-> a, c|-> a)$ 
посчитай $\sigma_1 \cdot \sigma_2$ и $\sigma_2 \cdot \sigma_1$ 
И покажи как ты это делаешь

Тут снова начинай давать примеры и дополняй информацией
Определение:
Группа - это моноид с особым свойством
1) $\forall a \in G:\exists 1/a \in G || (1/a)*a = e = a*(1/a) (e - нейтральный)$
M = Maps(X,X) - не группа
G = Bij(биекция)(X, X) все биекции X->X => группа симметрий мн-во X
	(тоже покажи мне как это работает)
Утверждение G - группа, тогда докажи что существует единственный обратный
	Допустим $\exists (1/a)_1 != (1/a)_1,  но (a)*_1 = (a)*_2$ 
	a$*$ $_1$ = e |  $*$=  $_2$ 
	$_1 = _2$ 
	
Снова давай примеры и дополняй информацией
Определение кольцо R - называется мн-во с двумя бинарными операциями
Обычно (+, $\cdot$)
2) R - комм группа
3) $\forall a,b,c \in R$ $, (a+b)\cdot c = a \cdot c + b \cdot c$  // $c \cdot (a+b) = c \cdot a + c \cdot b$  
Кольцо ассоциативно, когда $(a*b)*c = a*(b*c) || \forall a,b,c \in R$  
Кольцо с единицей, когда $a*1 = 1*a = a || \forall a \in R$ 
Коммутативное кольцо, если $a*b = b*a || \forall a,b \in R$ 
Для нас кольцо - ассоциативное кольцо с единицей

Пусть R - комм кольцо, что для любого a существует обратный элемент, тогда R - поле.
Докажи что если 0 != 1, то 0 не имеет обратного
//
Пусть R - кольцо. Тогда $0*a = a*0=0 || \forall a \in R$ 
Док-во: $0*a = (0+0)*a = 0*a+0*a$ 
$0*a+0*a=0*a*0*a-0*a = 0*a$ Тогда получается, что $0 = a*0$
//
Утверждение: $(-a)*b = -(a*b) = a*(-b) || \forall a,b \in R$ 
Док-во: хочу доказать $(-a)*b = -(a*b)$ | += $(a*b)$ 
$(a-a)*b = 0$ (пользуемся дистрибутивностью кольца)
т.е. 0 = 0
Аналогично докажем, что $-(a*b) = a*(-b)$ 
Если a = -1, то $-1*b = -b$ 

Основной курс начинается с $Z_Z$ . (след лекция)
Определение:
	R - кольцо
	$I$ in $R$ <- все числа которые делятся на 7
	Очевидно, что $a,b \in Z$  || $a+b \in Z$  || $a\in I$  $r \in R$  !!! => $r*a \in I$ 
	$I$ in R называется идеалом, если I - абелева группа  по сложению
		$\forall a \in I$  $\forall r \in R$ !!! => $r*a \in I$ 
	Пример:
		$R = Z$   $I = Z_n$ - все числа делсятся на $n$ 
	Элемент $a \in R$ назывется делителем 0, если $\exists b \in R : a*b = 0$ такое что b != 0
	Определение: коммутативное кольцо называется областью целлостности, если не существует делителей 0  
		  