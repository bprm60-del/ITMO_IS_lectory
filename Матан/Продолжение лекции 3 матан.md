(согласно всем инструкциям и дополняя информацией из интернета распиши)
(Добавь дз по этой лекции)
Счетность Q 
Q = {p/q $p \in Z$; $q \in N$}
|Q| равномощно N
Доказательство:
	Построим декартово произведение Z $*$ N; такое, что f: Z*N -> Z/N ; очевидно что их число счётно
Теорема Кантора-Булеана:
	Булеана - множество всех подмножеств A;                 = $2^A$
	 {1,2,3} = 8 … распиши все
	 {a} < $P(a)$ < $P(P(a))$ < . . . 
	 Доказательство:
		 Построим инъекцию A -> P(a)
		 Предположим что существует сюръекция g: A-> P(a)
		 Получается для любого x $\in$ P(a). $\exists$ x $\in A$ : g(x) = X
		 Построим B = {x $\in A$ | $x \notin g(x)$}
		 B $\in P(a)$ => $\exists b \in A$ : g(b) = B
		 Вопрос: $b \in B ?$ 
		 если b $\in$ B => b $\notin g(b) = B$
		 наоборот, если $b \notin B$ =>$b \in g(b) = B$
		Следствие:
			Существует бесконечно много мощностей

Поле вещественных чисел $R$ - это упорядоченное множество c действиями + , $*$. 
Удовлетворяющая 3 группам аксиом
Аксиомы поля:
a+b = b+a
$a*b = b*a$
$a*(b+c) = a*b+a*c$ 
$a*(b*c) = (a*b)*c$ 
(a+b) + c = a+(b+c)
$\exists 0$ 
$\exists 1$ 
$\exists {-a}$  
$\exists {1/a}$
0 != 1

Аксиомы порядка:
(Линейная упорядоченность) - множество называется упорядоченным полем, если оно поле и на нём введено отношение порядка ($<=$). Удолетворяющая следующим аксиомам
//
Рефлексивность $\forall a.a <= a$ 
Антисимметричность $\forall a, b$ если $a <= b. b >= a$ то $a=b$ 
Транзитивность $\forall a,b,c$ если $a<=b; b <=c$  то $a<=c$ 
//
Линейность: для любых a,b каждое или больше или меньше
//
Устойчивость сложения
//
Устойчивость умножения
a >= 0     b >= 0        => a*b >= 0 

Аксиома непрерывности:
	(Дедекинда):
	 Возьмём два отрезка A, B
	 Одно левее другого
	 Непрерывность гласит о том, что существует хоть одна точка c, такая что $\forall a \in A,\forall b \in B, (a <= b)..\exists c : a <= c <= b$ 

Упражнение-теорема (дз) {Q - не удовлетворяет аксиоме полноты} 
(по простому, что корень из двух не принадлежит Q)

Архимедовость поля R:
Принцип Архимеда:
	Множество N не ограничено в R. (+ выпиши это в дз с подробным доказательством)
	$\forall x \in R. \exists n \in N : x <= n$ 
	Доказательство:
		Пусть от противного
		Пусть есть та граница, тогда возьмём множество всех верхних границ R
		A = N; B - множество всех верхних границ
		Из этого следует по аксиоме непрерывности существует c; n <= c <= b; $\forall n \in A$ : b $\in$ B. Но получается по аксиоме пиано n+1 тоже принадлежит.
		Получается что c-1 $\in$ B. <=> противоречие c-1 >= c
	Упражнение (дз) выписать все следствия принципа Архимеда:
		1. $\forall e > 0; \exists n \in N; 1/n < e$ 
		2. Соизмеримость: $\forall x,y \in R_+; \exists n \in N; x*n > y$ 
		3. $\forall x \in R_+, h > 0 => \exists n \in N; n*h <= x < (n+1)*h$ 

Плотность рациональных и иррациональных чисел в множестве R (тоже в дз)
Теорема 1: В любом интервале найдётся рациональное число
Теорема 2: В любом интервале найдётся иррациональное число



	
		