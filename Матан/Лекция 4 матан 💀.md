(Исполни все предыдущие инструкции + выпиши доп задачи и все упражнения)
Принцип вложенных отрезков кантора
Теорема Кантора:
	Пускай для всех вложенных отрезков $I$. Тогда существует точка c которая принадлежит каждому отрезку $I$ 
	Кроме того, если $\forall \epsilon > 0. \exists N(\epsilon) \in N : \forall n > N(\epsilon)$  $b_n - a_n < \epsilon$ 
Доказательство:
	Пусть А = $\{a_n\}$ , B = $\{b_n\}$ . Для $\forall k \& m.. a_k < b_m$  => Для любого множества Дедекинда $\exists$ разделение . c. => $\forall k, m \in N. a_n <= c <= b_m$ => $\forall n \in N. a_n <= c <= b_n$ чтд.
	.
	.
	Докажем единственность от противного:
	Предположим $\exists c_1 < c_2 \in I_n \forall n$ ;;; d = $c_2-c_1$  => $|I_n| >= d$ $\forall n$ 
	Но если $\epsilon = d/2$ => противоречие. чтд.

Несчётность континуума:
	Мощность отрезка $card([0,1])$ обозначается буквой C. И называется континуумом
	Предположим что все числа можно занумеровать (остаток добавь в дз + допиши доказательство)

Множество:
Называется ограниченным сверху:
Называется ограниченных снизу:

Число M называется точной верхней границей E. Или супремумом $sup$ , если выполняются два условия на границы.  x - верхняя граница. $\forall \epsilon \exists x_{\epsilon} \in E: x_{\epsilon} < M - \epsilon$ 
Аналогично для нижней границы

Всякое непустое множество имеет супремум:
Док-во:
	E $\subset R$    E - ограничено сверху
	Пускай B - множество всех верхних границ 
	Существует c , что $\forall E, B.. e <= c <= b$. c - супремум т.к. оба условия из определения супремума выполнены

Аналогично выпиши для инфимума



Числовая последовательность
Последовательность - отображение $f (N)$ -> $R$    f(n) = x;    обозначается {$x_n$ $n=1 -> \inf$ }

Предел последовательности. 
Число a .. $a \in R$ называется пределом последовательности x, если $\forall \epsilon > 0;; \exists N(\epsilon) \in N : \forall n > N(\epsilon) ''' |x_n - a| < \epsilon$  

Единственность предела:
	Сходящаяся последовательность имеет единственный предел
	Докажем от противного:
	Пусть $lim(x_n) = a$ 
	И $lim(x_n) = b$ 
	Причём $a \not= b$ 
	Пусть $\epsilon = |b-a|/3$ Тогда $\exists N_1: \forall n > N_1(\epsilon):: |x_n - a| < \epsilon$ 
	аналогично и для b $\exists N_2$ 
	d = |b-a| = $|(b-x_n) - (a-x_n)|$ <=$|b-x_n| + |a-x_n|$ < N = max(N1, N2)
	$\epsilon + \epsilon = 2/3 d$ 

Ограниченность
Теорема: Если последовательность сходится, то она ограничена. 

Сохранение знака (отделимость от нуля):
Теорема: $lim x_n = a$ ; a > 0 => $\exists N \in N :: \forall n > N ;; x_n > a/2 > 0$ 
Доказательство:
$\epsilon = a/2$ 
$|x_n-a|$ < a/2 для n > $N(\epsilon)$
-a/2 < $x_n-a$ < a/2
$a/2 < x_n < 3a/2$

Бесконечно малые последовательности:
Определение: Последовательность называют бесконечно малой, если её предел равен 0
Теорема: Линейность и устойчивость бесконечно малых последовательностей.
	Пускай {a_n} и {b_n} - бесконечно малые :: a_n +- b_n - тоже бесконечно малая
	$\forall \lambda \in R$ $\lambda * a_n$ - тоже бесконечно малая 
	Для любой ограниченной последовательности выполняется a_n * x_n - бесконечно малая
	Доказательство:
		$\exists N_1 \forall n > N_1 .. |a_n| < \epsilon/2$ 
		$\exists N_2 : \forall n > N_2 .. |b_n| < \epsilon /2$ 
		====> $a_n +- b_n$ <= $|a_n| + b_n  < e/2 + e/2 = e$
	Докажи и покажи оставшийся пункты

(Доказательство - упражнения )
Арифметические свойства пределов: (Критерий существование предела)
$\exists lim(x_n) = a$ <=> x_n = a + d_n где d_n - бесконечное малая последовательность

Теорема:
lim a_n = a; lim b_n = b ===>>>
lim(a_n+-b_n) = a + b
lim(a_n$*$b_n) = a*b
если b != 0: действительно и обратное для умножения

Доказательство: a_n = a + d_n  ;;; b_n = b + $\beta_n$ где d и бэта - бесконечно малые последовательности => lim(a_n+b_n) = lim(a+b+d_n+\beta_n) 
Оставшиеся пункты докажи и покажи

$a_n / b_n$ = $(a+\alpha _n)/(b + \beta _n)$ 
$a_n/b_n - a/b = ((a+a_n)*b-a(b*b_n))/(b + b_n) * b =$ бесконечно малый / бесконечно малая

Теорема о двух милиционерах (распиши пж очень подробно, можешь даже в целом дать задачу)


Теорема Вейерштрасса:
Монотонная ограниченная последовательность сходится. (имеет предел)
Доказательство:
	Пусть последовательность неубывающая {x_n} множество в R ограниченно => 
	существует sup x_n  = a
	Докажем, что a = lim x_n
	sup x_n => 1) x_n < = a < a+$\epsilon$ 
	2) существует $x_N$ такое что a-e => $\forall b > N .. a-e < x_n < a+e$ 